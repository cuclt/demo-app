<ul class="nav nav-pills nav-stacked users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 50 %><br>
      <%= link_to user.name.truncate(15), user %>
    </li>
  <% end %>
</ul>
<%= will_paginate @users %>
